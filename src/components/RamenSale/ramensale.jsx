import PropTypes from 'prop-types';
import React from 'react';

function RamenSale() {

    return (
        <div>
            minion XD
  </div>
    )
  }

  RamenSale.propTypes = {
    id: PropTypes.string,
    name: PropTypes.string,
    spice: PropTypes.string,
    price: PropTypes.number,
}

  export default RamenSale